version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: pim-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: pim_db
      MYSQL_USER: pim_user
      MYSQL_PASSWORD: pim_pass123
    ports:
      - "3307:3306"  # ← BURASI DEĞİŞTİ! 3307'ye taşıdık
    volumes:
      - mysql_data:/var/lib/mysql
    command:
      - --default-authentication-plugin=mysql_native_password
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot123"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pim-network

volumes:
  mysql_data:

networks:
  pim-network:
    driver: bridge